services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    env_file:
      - backend/.env
    ports:
      - "8888:8888"
    volumes:
      - ./data:/app/data
    environment:
      DATABASE_PATH: /app/data/xuanshu.db
    restart: unless-stopped
